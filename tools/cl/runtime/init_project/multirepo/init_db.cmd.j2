@echo off

call activate

set PYTHONPATH={% for package_dir in package_dirs %}.\{{ package_dir }};{% endfor %}%PYTHONPATH%

echo.
echo Initialize DB (error if not a DEV or TEST DB)
python -m tools.cl.runtime.init_db

